<!-- Credit Card Benefits Section -->
<section class="card-benefits">
    <div class="card-benefits__container">
        <div class="card-benefits__header">
            <h2 class="card-benefits__title">{{ tp('banking_cards.benefits.title', [], 'banking_cards') }}</h2>
            <p class="card-benefits__subtitle">
                {{ tp('banking_cards.benefits.subtitle', [], 'banking_cards') }}
            </p>
        </div>
        
        <div class="card-benefits__grid">
            <div class="card-benefits__main">
                <div class="card-benefits__highlight">
                    <div class="card-benefits__highlight-visual">
                        <div class="card-benefits__cashback-circle">
                            <div class="card-benefits__cashback-inner">
                                <div class="card-benefits__cashback-amount">5%</div>
                                <div class="card-benefits__cashback-label">Cashback</div>
                            </div>
                            <div class="card-benefits__cashback-ring"></div>
                        </div>
                        <div class="card-benefits__floating-coins">
                            <div class="card-benefits__coin" style="--delay: 0s; --x: 20%; --y: 30%;">
                                {{ ux_icon('heroicons:banknotes', {'class': 'card-benefits__coin-icon'}) }}
                            </div>
                            <div class="card-benefits__coin" style="--delay: 1s; --x: 80%; --y: 20%;">
                                {{ ux_icon('heroicons:banknotes', {'class': 'card-benefits__coin-icon'}) }}
                            </div>
                            <div class="card-benefits__coin" style="--delay: 2s; --x: 60%; --y: 70%;">
                                {{ ux_icon('heroicons:banknotes', {'class': 'card-benefits__coin-icon'}) }}
                            </div>
                        </div>
                    </div>
                    <div class="card-benefits__highlight-content">
                        <h3 class="card-benefits__highlight-title">{{ tp('banking_cards.benefits.cashback.title', [], 'banking_cards') }}</h3>
                        <p class="card-benefits__highlight-desc">
                            {{ tp('banking_cards.benefits.cashback.description', [], 'banking_cards') }}
                        </p>
                        <div class="card-benefits__highlight-stats">
                            <div class="card-benefits__highlight-stat">
                                <span class="card-benefits__stat-number">{{ tp('banking_cards.benefits.cashback.stats.average_yearly', [], 'banking_cards') }}</span>
                                <span class="card-benefits__stat-label">{{ tp('banking_cards.benefits.cashback.stats.average_yearly_label', [], 'banking_cards') }}</span>
                            </div>
                            <div class="card-benefits__highlight-stat">
                                <span class="card-benefits__stat-number">{{ tp('banking_cards.benefits.cashback.stats.payment_time', [], 'banking_cards') }}</span>
                                <span class="card-benefits__stat-label">{{ tp('banking_cards.benefits.cashback.stats.payment_time_label', [], 'banking_cards') }}</span>
                            </div>
                        </div>
                        <div class="card-benefits__highlight-cta">
                            <a href="{{ path('app_login') }}" class="card-benefits__cta-button">
                                <span class="card-benefits__cta-text">{{ tp('banking_cards.benefits.cashback.cta', [], 'banking_cards') }}</span>
                                <span class="card-benefits__cta-icon">
                                    {{ ux_icon('heroicons:arrow-right', {'class': 'card-benefits__cta-arrow'}) }}
                                </span>
                            </a>
                        </div>
                    </div>
                </div>
            </div>
            
            <div class="card-benefits__bento">
                <div class="card-benefits__bento-row">
                    <div class="card-benefits__feature">
                        <div class="card-benefits__feature-icon">
                            {{ ux_icon('heroicons:shield-check', {'class': 'card-benefits__icon'}) }}
                        </div>
                        <h4 class="card-benefits__feature-title">{{ tp('banking_cards.benefits.features.insurance.title', [], 'banking_cards') }}</h4>
                        <p class="card-benefits__feature-desc">
                            {{ tp('banking_cards.benefits.features.insurance.description', [], 'banking_cards') }}
                        </p>
                    </div>
                    
                    <div class="card-benefits__feature">
                        <div class="card-benefits__feature-icon">
                            {{ ux_icon('heroicons:building-office', {'class': 'card-benefits__icon'}) }}
                        </div>
                        <h4 class="card-benefits__feature-title">{{ tp('banking_cards.benefits.features.vip_lounges.title', [], 'banking_cards') }}</h4>
                        <p class="card-benefits__feature-desc">
                            {{ tp('banking_cards.benefits.features.vip_lounges.description', [], 'banking_cards') }}
                        </p>
                    </div>
                </div>
                
                <div class="card-benefits__bento-row">
                    <div class="card-benefits__feature">
                        <div class="card-benefits__feature-icon">
                            {{ ux_icon('heroicons:phone', {'class': 'card-benefits__icon'}) }}
                        </div>
                        <h4 class="card-benefits__feature-title">{{ tp('banking_cards.benefits.features.concierge.title', [], 'banking_cards') }}</h4>
                        <p class="card-benefits__feature-desc">
                            {{ tp('banking_cards.benefits.features.concierge.description', [], 'banking_cards') }}
                        </p>
                    </div>
                    
                    <div class="card-benefits__feature">
                        <div class="card-benefits__feature-icon">
                            {{ ux_icon('heroicons:globe-europe-africa', {'class': 'card-benefits__icon'}) }}
                        </div>
                        <h4 class="card-benefits__feature-title">{{ tp('banking_cards.benefits.features.worldwide_acceptance.title', [], 'banking_cards') }}</h4>
                        <p class="card-benefits__feature-desc">
                            {{ tp('banking_cards.benefits.features.worldwide_acceptance.description', [], 'banking_cards') }}
                        </p>
                    </div>
                </div>
                
                <div class="card-benefits__bento-row">
                    <div class="card-benefits__feature">
                        <div class="card-benefits__feature-icon">
                            {{ ux_icon('heroicons:lock-closed', {'class': 'card-benefits__icon'}) }}
                        </div>
                        <h4 class="card-benefits__feature-title">{{ tp('banking_cards.benefits.features.security.title', [], 'banking_cards') }}</h4>
                        <p class="card-benefits__feature-desc">
                            {{ tp('banking_cards.benefits.features.security.description', [], 'banking_cards') }}
                        </p>
                    </div>
                    
                    <div class="card-benefits__feature">
                        <div class="card-benefits__feature-icon">
                            {{ ux_icon('heroicons:gift', {'class': 'card-benefits__icon'}) }}
                        </div>
                        <h4 class="card-benefits__feature-title">{{ tp('banking_cards.benefits.features.exclusive_offers.title', [], 'banking_cards') }}</h4>
                        <p class="card-benefits__feature-desc">
                            {{ tp('banking_cards.benefits.features.exclusive_offers.description', [], 'banking_cards') }}
                        </p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

<style>
/* =================================================================
   CREDIT CARD - BENEFITS SECTION
   ================================================================= */

/* Block: card-benefits */
.card-benefits {
    padding: 6rem 0;
    background: var(--color-background);
    position: relative;
    overflow: hidden;
}

.card-benefits::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: radial-gradient(circle at 20% 80%, rgba(229, 90, 43, 0.1) 0%, transparent 50%),
                radial-gradient(circle at 80% 20%, rgba(251, 191, 36, 0.1) 0%, transparent 50%);
    pointer-events: none;
}

/* Element: card-benefits__container */
.card-benefits__container {
    max-width: 1400px;
    margin: 0 auto;
    padding: 0 2rem;
    position: relative;
    z-index: 2;
}

/* Element: card-benefits__header */
.card-benefits__header {
    text-align: center;
    margin-bottom: 4rem;
    max-width: 800px;
    margin-left: auto;
    margin-right: auto;
}

/* Element: card-benefits__title */
.card-benefits__title {
    font-size: clamp(2.5rem, 5vw, 3.5rem);
    font-weight: 800;
    color: var(--color-text);
    margin-bottom: 1.5rem;
    line-height: 1.2;
    letter-spacing: -0.02em;
}

/* Element: card-benefits__subtitle */
.card-benefits__subtitle {
    font-size: clamp(1.125rem, 2.5vw, 1.375rem);
    color: var(--color-text-muted);
    line-height: 1.7;
    font-weight: 400;
}

/* Element: card-benefits__grid */
.card-benefits__grid {
    display: flex;
    flex-direction: row;
    gap: 4rem;
    align-items: start;
}

/* Element: card-benefits__main */
.card-benefits__main {
    position: relative;
    flex: 1;
}

/* Element: card-benefits__highlight */
.card-benefits__highlight {
    background: white;
    border-radius: 2rem;
    padding: 3rem;
    box-shadow: 0 20px 25px rgba(0, 0, 0, 0.1);
    border: 1px solid rgba(0, 0, 0, 0.05);
    position: relative;
    overflow: hidden;
}

.card-benefits__highlight::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: linear-gradient(135deg, rgba(229, 90, 43, 0.02) 0%, rgba(251, 191, 36, 0.02) 100%);
    pointer-events: none;
}

/* Element: card-benefits__highlight-visual */
.card-benefits__highlight-visual {
    position: relative;
    height: 200px;
    margin-bottom: 2rem;
    display: flex;
    align-items: center;
    justify-content: center;
}

/* Element: card-benefits__cashback-circle */
.card-benefits__cashback-circle {
    position: relative;
    width: 150px;
    height: 150px;
    display: flex;
    align-items: center;
    justify-content: center;
}

/* Element: card-benefits__cashback-inner */
.card-benefits__cashback-inner {
    width: 120px;
    height: 120px;
    background: linear-gradient(135deg, #e55a2b 0%, #fbbf24 100%);
    border-radius: 50%;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    color: white;
    position: relative;
    z-index: 2;
    box-shadow: 0 15px 30px rgba(229, 90, 43, 0.3);
}

/* Element: card-benefits__cashback-amount */
.card-benefits__cashback-amount {
    font-size: 3rem;
    font-weight: 900;
    line-height: 1;
    margin-bottom: 0.25rem;
}

/* Element: card-benefits__cashback-label */
.card-benefits__cashback-label {
    font-size: 0.875rem;
    font-weight: 700;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    opacity: 0.9;
}

/* Element: card-benefits__cashback-ring */
.card-benefits__cashback-ring {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    border: 3px solid;
    border-color: #e55a2b transparent #fbbf24 transparent;
    border-radius: 50%;
    animation: rotate 8s linear infinite;
}

@keyframes rotate {
    from { transform: rotate(0deg); }
    to { transform: rotate(360deg); }
}

/* Element: card-benefits__floating-coins */
.card-benefits__floating-coins {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    pointer-events: none;
}

/* Element: card-benefits__coin */
.card-benefits__coin {
    position: absolute;
    top: var(--y);
    left: var(--x);
    width: 2rem;
    height: 2rem;
    background: linear-gradient(135deg, #fbbf24 0%, #f59e0b 100%);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    animation: float 3s ease-in-out infinite;
    animation-delay: var(--delay);
    opacity: 0.8;
}

@keyframes float {
    0%, 100% { transform: translateY(0px); }
    50% { transform: translateY(-20px); }
}

/* Element: card-benefits__coin-icon */
.card-benefits__coin-icon {
    width: 1rem;
    height: 1rem;
    color: white;
}

/* Element: card-benefits__highlight-content */
.card-benefits__highlight-content {
    position: relative;
    z-index: 2;
}

/* Element: card-benefits__highlight-title */
.card-benefits__highlight-title {
    font-size: 2rem;
    font-weight: 800;
    color: var(--color-text);
    margin-bottom: 1rem;
    line-height: 1.3;
}

/* Element: card-benefits__highlight-desc */
.card-benefits__highlight-desc {
    font-size: 1.125rem;
    color: var(--color-text-muted);
    line-height: 1.7;
    margin-bottom: 2rem;
}

/* Element: card-benefits__highlight-stats */
.card-benefits__highlight-stats {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 2rem;
}

/* Element: card-benefits__highlight-stat */
.card-benefits__highlight-stat {
    text-align: center;
    padding: 1.5rem;
    background: rgba(229, 90, 43, 0.05);
    border-radius: 1rem;
    border: 1px solid rgba(229, 90, 43, 0.1);
}

/* Element: card-benefits__stat-number */
.card-benefits__stat-number {
    display: block;
    font-size: 1.75rem;
    font-weight: 800;
    background: linear-gradient(135deg, #e55a2b 0%, #fbbf24 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    margin-bottom: 0.5rem;
}

/* Element: card-benefits__stat-label */
.card-benefits__stat-label {
    font-size: 0.875rem;
    color: var(--color-text-muted);
    font-weight: 600;
}

/* Element: card-benefits__highlight-cta */
.card-benefits__highlight-cta {
    margin-top: 2.5rem;
    text-align: center;
}

/* Element: card-benefits__cta-button */
.card-benefits__cta-button {
    display: inline-flex;
    align-items: center;
    gap: 0.75rem;
    padding: 1rem 2rem;
    background: linear-gradient(135deg, #e55a2b 0%, #f59e0b 100%);
    color: white;
    text-decoration: none;
    font-weight: 700;
    font-size: 1.125rem;
    border-radius: 3rem;
    box-shadow: 0 8px 32px rgba(229, 90, 43, 0.3);
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    position: relative;
    overflow: hidden;
}

.card-benefits__cta-button::before {
    content: '';
    position: absolute;
    top: 0;
    left: -100%;
    right: 0;
    bottom: 0;
    background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
    transition: left 0.6s ease;
}

.card-benefits__cta-button:hover {
    transform: translateY(-2px);
    box-shadow: 0 12px 40px rgba(229, 90, 43, 0.4);
}

.card-benefits__cta-button:hover::before {
    left: 100%;
}

.card-benefits__cta-button:active {
    transform: translateY(0);
}

/* Element: card-benefits__cta-text */
.card-benefits__cta-text {
    font-weight: 700;
    letter-spacing: 0.025em;
}

/* Element: card-benefits__cta-arrow */
.card-benefits__cta-arrow {
    width: 1.25rem;
    height: 1.25rem;
    transition: transform 0.3s ease;
}

.card-benefits__cta-button:hover .card-benefits__cta-arrow {
    transform: translateX(4px);
}

/* Element: card-benefits__bento */
.card-benefits__bento {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
    flex: 1;
}

/* Element: card-benefits__bento-row */
.card-benefits__bento-row {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1.5rem;
}

/* Element: card-benefits__feature */
.card-benefits__feature {
    background: white;
    padding: 2rem;
    border-radius: 1.5rem;
    border: 1px solid rgba(0, 0, 0, 0.05);
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.05);
    transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    position: relative;
    overflow: hidden;
}

.card-benefits__feature::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 4px;
    background: linear-gradient(90deg, #e55a2b 0%, #fbbf24 100%);
    transform: translateX(-100%);
    transition: transform 0.3s ease;
}

.card-benefits__feature:hover {
    transform: translateY(-4px);
    box-shadow: 0 20px 25px rgba(0, 0, 0, 0.1);
}

.card-benefits__feature:hover::before {
    transform: translateX(0);
}

/* Element: card-benefits__feature-icon */
.card-benefits__feature-icon {
    width: 3rem;
    height: 3rem;
    background: linear-gradient(135deg, #e55a2b 0%, #fbbf24 100%);
    border-radius: 1rem;
    display: flex;
    align-items: center;
    justify-content: center;
    margin-bottom: 1.5rem;
    box-shadow: 0 8px 16px rgba(229, 90, 43, 0.2);
}

/* Element: card-benefits__icon */
.card-benefits__icon {
    width: 1.5rem;
    height: 1.5rem;
    color: white;
}

/* Element: card-benefits__feature-title */
.card-benefits__feature-title {
    font-size: 1.25rem;
    font-weight: 700;
    color: var(--color-text);
    margin-bottom: 1rem;
    line-height: 1.4;
}

/* Element: card-benefits__feature-desc */
.card-benefits__feature-desc {
    color: var(--color-text-muted);
    line-height: 1.6;
    font-size: 0.95rem;
}

/* Responsive Design */
@media (max-width: 1200px) {
    .card-benefits__container {
        padding: 0 1.5rem;
    }
    
    .card-benefits__grid {
        gap: 3rem;
    }
    
    .card-benefits__highlight {
        padding: 2.5rem;
    }
    
    .card-benefits__feature {
        padding: 1.75rem;
    }
}

@media (max-width: 1024px) {
    .card-benefits {
        padding: 4rem 0;
    }
    
    .card-benefits__grid {
        flex-direction: column;
        gap: 3rem;
    }
    
    .card-benefits__main {
        order: 1;
    }
    
    .card-benefits__bento {
        order: 2;
    }
    
    .card-benefits__bento-row {
        grid-template-columns: 1fr;
        gap: 1.25rem;
    }
    
    .card-benefits__highlight {
        padding: 2.5rem;
    }
    
    .card-benefits__cashback-circle {
        width: 120px;
        height: 120px;
    }
    
    .card-benefits__cashback-inner {
        width: 100px;
        height: 100px;
    }
    
    .card-benefits__cashback-amount {
        font-size: 2.5rem;
    }
}

@media (max-width: 768px) {
    .card-benefits {
        padding: 3rem 0;
    }
    
    .card-benefits__container {
        padding: 0 1.5rem;
    }
    
    .card-benefits__header {
        margin-bottom: 3rem;
    }
    
    .card-benefits__grid {
        gap: 2rem;
    }
    
    .card-benefits__bento-row {
        grid-template-columns: 1fr;
        gap: 1rem;
    }
    
    .card-benefits__highlight {
        padding: 2rem;
    }
    
    .card-benefits__highlight-visual {
        height: 160px;
        margin-bottom: 1.5rem;
    }
    
    .card-benefits__cashback-circle {
        width: 100px;
        height: 100px;
    }
    
    .card-benefits__cashback-inner {
        width: 80px;
        height: 80px;
    }
    
    .card-benefits__cashback-amount {
        font-size: 2rem;
    }
    
    .card-benefits__highlight-title {
        font-size: 1.75rem;
    }
    
    .card-benefits__highlight-stats {
        grid-template-columns: 1fr;
        gap: 1rem;
    }
    
    .card-benefits__cta-button {
        padding: 0.875rem 1.75rem;
        font-size: 1rem;
    }
    
    .card-benefits__feature {
        padding: 1.5rem;
    }
    
    .card-benefits__feature-icon {
        width: 2.5rem;
        height: 2.5rem;
        margin-bottom: 1rem;
    }
    
    .card-benefits__icon {
        width: 1.25rem;
        height: 1.25rem;
    }
    
    .card-benefits__feature-title {
        font-size: 1.125rem;
    }
    
    .card-benefits__feature-desc {
        font-size: 0.9rem;
    }
}

@media (max-width: 640px) {
    .card-benefits {
        padding: 2.5rem 0;
    }
    
    .card-benefits__container {
        padding: 0 1.25rem;
    }
    
    .card-benefits__highlight {
        padding: 1.75rem;
    }
    
    .card-benefits__highlight-visual {
        height: 140px;
        margin-bottom: 1.25rem;
    }
    
    .card-benefits__cta-button {
        padding: 0.8rem 1.5rem;
        font-size: 0.95rem;
        width: 100%;
        justify-content: center;
    }
    
    .card-benefits__feature {
        padding: 1.25rem;
    }
    
    .card-benefits__cashback-circle {
        width: 90px;
        height: 90px;
    }
    
    .card-benefits__cashback-inner {
        width: 72px;
        height: 72px;
    }
    
    .card-benefits__cashback-amount {
        font-size: 1.8rem;
    }
    
    .card-benefits__cashback-label {
        font-size: 0.75rem;
    }
}

@media (max-width: 480px) {
    .card-benefits__container {
        padding: 0 1rem;
    }
    
    .card-benefits__highlight {
        padding: 1.5rem;
    }
    
    .card-benefits__highlight-visual {
        height: 120px;
        margin-bottom: 1rem;
    }
    
    .card-benefits__feature {
        padding: 1rem;
    }
    
    .card-benefits__coin {
        width: 1.5rem;
        height: 1.5rem;
    }
    
    .card-benefits__coin-icon {
        width: 0.75rem;
        height: 0.75rem;
    }
    
    .card-benefits__cta-button {
        padding: 0.75rem 1.5rem;
        font-size: 0.9rem;
        width: 100%;
        justify-content: center;
    }
    
    .card-benefits__highlight-title {
        font-size: 1.5rem;
        margin-bottom: 0.75rem;
    }
    
    .card-benefits__highlight-desc {
        font-size: 1rem;
        margin-bottom: 1.5rem;
    }
    
    .card-benefits__feature-title {
        font-size: 1rem;
        margin-bottom: 0.75rem;
    }
    
    .card-benefits__feature-desc {
        font-size: 0.85rem;
        line-height: 1.5;
    }
    
    .card-benefits__feature-icon {
        width: 2.25rem;
        height: 2.25rem;
        margin-bottom: 0.75rem;
    }
    
    .card-benefits__icon {
        width: 1.125rem;
        height: 1.125rem;
    }
}
</style>
