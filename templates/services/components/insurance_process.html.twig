<section class="insurance-process">
    <h2 class="insurance-process__title">{{ tp('insurance.process.title', [], 'insurance') }}</h2>
    <ol class="insurance-process__steps">
        <li>
            <div class="insurance-process__icon">
                <svg fill="none" stroke="currentColor" viewBox="0 0 24 24"><circle cx="12" cy="12" r="10" stroke-width="2"/><path d="M8 12l2 2 4-4" stroke-width="2"/></svg>
            </div>
            <span>{{ tp('insurance.process.steps.choose.title', [], 'insurance') }}</span>
        </li>
        <li>
            <div class="insurance-process__icon">
                <svg fill="none" stroke="currentColor" viewBox="0 0 24 24"><rect x="4" y="4" width="16" height="16" rx="2" stroke-width="2"/><path d="M8 11h8M8 15h6" stroke-width="2"/></svg>
            </div>
            <span>{{ tp('insurance.process.steps.form.title', [], 'insurance') }}</span>
        </li>
        <li>
            <div class="insurance-process__icon">
                <svg fill="none" stroke="currentColor" viewBox="0 0 24 24"><path d="M12 8v4l3 3" stroke-width="2"/><circle cx="12" cy="12" r="10" stroke-width="2"/></svg>
            </div>
            <span>{{ tp('insurance.process.steps.proposal.title', [], 'insurance') }}</span>
        </li>
        <li>
            <div class="insurance-process__icon">
                <svg fill="none" stroke="currentColor" viewBox="0 0 24 24"><path d="M5 13l4 4L19 7" stroke-width="2"/></svg>
            </div>
            <span>{{ tp('insurance.process.steps.subscribe.title', [], 'insurance') }}</span>
        </li>
    </ol>
    <style>
        .insurance-process {
            padding: 3rem 0 2rem 0;
            background: var(--color-bg, #fff);
        }
        .insurance-process__title {
            text-align: center;
            font-size: 2rem;
            margin-bottom: 2.5rem;
            color: var(--color-primary, #1e293b);
        }
        .insurance-process__steps {
            display: flex;
            justify-content: center;
            gap: 2.5rem;
            list-style: none;
            padding: 0;
            margin: 0 auto;
            max-width: 1100px;
        }
        .insurance-process__steps li {
            display: flex;
            flex-direction: column;
            align-items: center;
            flex: 1 1 0;
            min-width: 180px;
        }
        .insurance-process__icon {
            width: 4rem;
            height: 4rem;
            border-radius: 50%;
            background: var(--color-primary, #6366f1);
            color: #fff;
            display: flex;
            align-items: center;
            justify-content: center;
            margin-bottom: 1rem;
        }
        .insurance-process__icon svg {
            width: 2.2rem;
            height: 2.2rem;
        }
        .insurance-process__steps span {
            font-size: 1.1rem;
            color: var(--color-text, #334155);
            text-align: center;
        }
        @media (max-width: 900px) {
            .insurance-process__steps {
                flex-direction: column;
                gap: 1.5rem;
                align-items: center;
            }
            .insurance-process__steps li {
                min-width: 0;
            }
        }
    </style>
</section>
